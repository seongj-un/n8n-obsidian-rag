# docker compose 가 읽는다. 이 파일을 .env 로 복사해 값을 채울 것.
# .env 는 .gitignore 에 있다.

# 옵시디언 볼트에서 색인할 폴더의 **호스트 절대 경로**
VAULT_PATH=/path/to/your/obsidian-vault

# pgvector 비밀번호 (아무 값이나 정하면 된다)
VECTORDB_PASSWORD=change-me
